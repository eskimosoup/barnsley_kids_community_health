<%= form_for([@pathway_section, @pathway_jigsaw_piece]) do |f| %>
  <% if @pathway_jigsaw_piece.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pathway_jigsaw_piece.errors.count, "error") %> prohibited this pathway jigsaw piece from being saved:</h2>

      <ul>
        <% @pathway_jigsaw_piece.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :colour %><br>
    <%= f.select :colour, PathwayJigsawPiece::COLOURS %>
  </div>

  <div class="field">
    <%= f.label :display %><br>
    <%= f.check_box :display %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
